// Package yamlstore implements configuration.Store with the physical store's
// versioned config.yaml document. It translates that external representation
// into typed overrides; package configuration owns precedence and resolution.
package yamlstore
