// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: store_verification.sql

package query

import (
	"context"
)

const storeCountInvalidClaims = `-- name: StoreCountInvalidClaims :one
SELECT count(*)
FROM active_claims AS claim
JOIN issues AS issue ON issue.id = claim.issue_id
WHERE issue.lifecycle <> 'open'
    OR issue.kind NOT IN ('workstream', 'task', 'routine')
`

func (q *Queries) StoreCountInvalidClaims(ctx context.Context) (int64, error) {
	row := q.db.QueryRowContext(ctx, storeCountInvalidClaims)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const storeCountInvalidProjectionRevisions = `-- name: StoreCountInvalidProjectionRevisions :one
SELECT count(*)
FROM boards AS board
LEFT JOIN issues AS issue ON issue.board_id = board.id
WHERE board.revision > ?1
    OR issue.revision > board.revision
`

func (q *Queries) StoreCountInvalidProjectionRevisions(ctx context.Context, currentRevision int64) (int64, error) {
	row := q.db.QueryRowContext(ctx, storeCountInvalidProjectionRevisions, currentRevision)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const storeCountStateRows = `-- name: StoreCountStateRows :one
SELECT count(*)
FROM store_state
`

func (q *Queries) StoreCountStateRows(ctx context.Context) (int64, error) {
	row := q.db.QueryRowContext(ctx, storeCountStateRows)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const storeGetVerificationState = `-- name: StoreGetVerificationState :one
SELECT current_revision, next_issue_number
FROM store_state
WHERE singleton = 1
`

type StoreGetVerificationStateRow struct {
	CurrentRevision int64
	NextIssueNumber int64
}

func (q *Queries) StoreGetVerificationState(ctx context.Context) (StoreGetVerificationStateRow, error) {
	row := q.db.QueryRowContext(ctx, storeGetVerificationState)
	var i StoreGetVerificationStateRow
	err := row.Scan(&i.CurrentRevision, &i.NextIssueNumber)
	return i, err
}
