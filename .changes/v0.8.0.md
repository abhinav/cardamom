## <a name="v0.8.0">v0.8.0</a> - 2026-08-14
### Added
- Added ordered board pins with CLI and web controls, inherited limits, copy and backup preservation, and pin IDs and titles in `claim --context` and `show --context` output.
- Allow viewing read-only board context from every board selector entry without changing the selected board.
### Changed
- skill: Make acceptance evidence, investigation decision rules, outcome lineage, custody handoffs, and next actions explicit.
### Fixed
- Accept an optional `%` prefix on CLI issue ID references.
