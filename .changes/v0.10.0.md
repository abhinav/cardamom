## <a name="v0.10.0">v0.10.0</a> - 2026-09-05
### Added
- Add `card search` for ranked full-text issue discovery with record and metadata filters.
### Changed
- Allow `card log show` to read one Log entry by its stable `log_` or historical `cmt_` ID.
